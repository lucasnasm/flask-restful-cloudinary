## Upload de arquivos via API com flask-restful e Cloudinary

```
python3 -m venv venv
pip install -r requirements.txt
source venv/bin/activate
python run.py
```
**Use curl or insomnia for upload file**